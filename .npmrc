# Force install of devDependencies regardless of NODE_ENV.
# Some environments set NODE_ENV=production globally, which makes npm default
# to omit=dev and silently drop devDependencies. See docs/POST_M1_FOLLOWUPS.md #9.
include=dev
